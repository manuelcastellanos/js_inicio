// if ternario
// expression? si true : si false
// Comparar si el primer numero es mayor que el segundo
// imprimir el primero
// sino
//imprimir el segundo

const NUMBER_ONE = parseInt( prompt('Pon un numero we'))
const NUMBER_TWO = parseInt( prompt('Ahora pon otro man'))

NUMBER_ONE > NUMBER_TWO ? 
    console.log (`El ${NUMBER_ONE} es el mayor`) 
    : 
    console.log (`El ${NUMBER_TWO} es el mayor`)